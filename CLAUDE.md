# B1-2 React 서비스 프로젝트 — Claude Code 지침

이 문서는 구현·검증·보고 규칙의 원본이다. 별도 전역 지침에 의존하지 않는다.

## 역할과 범위

- 한국어로 소통하고 사용자 지시를 우선한다. Codex가 위임한 목표·범위·제약·완료 기준에 따라 구현·검증·학습자료를 맡는다.
- [AGENTS.md](AGENTS.md)의 `프로젝트 공통 조건`만 읽어 기술 제약·소스 경로·브라우저 기준을 따른다. Codex 전용 절차는 적용하지 않는다.
- 작업 전 [진행 상태](docs/progress.md)의 현재 상태와 해당 체크리스트를 확인해 하나의 학습 목적에 필요한 작업만 한다. 불명확한 요구사항만 `docs/Requirement_B1-2.pdf`로 확인한다.
- Git 상태를 확인하고 기존 변경을 보존한다. 지정 작업트리 밖은 수정하지 않고, 위임 작업은 add·커밋·푸시 없이 남긴다. 사용자가 직접 요청한 Git 작업은 그 지시를 따른다.
- 기존 구조를 따르고 불필요한 기능·추상화·의존성을 추가하지 않는다. 범위 안의 명확한 오류는 처리하되, 불명확한 요구나 범위 확대는 문제·추천·진행 상태를 Codex에 보고한다.
- 루트 README는 사용자 요청이 있을 때만 읽기·검토·수정한다. 작업 효율 개선도 프로젝트 지침에서 관리하며, 관찰한 비효율은 완료 보고의 `efficiency` 한 줄로 알리고 위임 범위 밖의 개선점은 Codex에 보고한다.

## 단계와 맥락

- 구현·검증 후 검토 근거를 보고하고 끝낸다. 학습자료는 코드 검토·보완이 끝난 뒤 별도 위임받아 작성한다. 자료 단계에서 코드 결함을 찾으면 위치·영향을 보고해 보완 단계로 돌린다.
- 같은 작업의 후속 보완은 기존 지침·코드 맥락·검증 근거를 재사용한다. 새 세션에서는 전달받은 요약부터 확인하고 필요한 파일·함수·구간만 읽는다. 변경이나 불확실성이 없으면 읽었던 자료를 일괄 재독하지 않는다.
- 독립적인 확인은 묶어서 실행한다. 보고·자료·주석의 같은 설명과 코드 전문·긴 로그를 반복하지 않는다.
- 기능 체크박스는 구현·검증 결과에 맞추고 현재 상태에 검토·자료 대기를 적는다. 학습 단위 완료·최신 자료·다음 작업·갱신일은 검토와 자료 반영 뒤 마감한다. 지침만 바꾸면 기능 체크박스는 바꾸지 않는다. 진행 상태는 요구사항·진행 상태의 원본이므로 효율 관찰·사용량은 적지 않는다.

## 검증

- 기존 도구의 필터나 간단한 인자로 시나리오별 선택 실행이 가능하게 한다. 별도 프레임워크를 만들지 않고, 기존 스크립트는 필요한 검사 추출 또는 최소 필터 추가로 재사용한다.
- 보완 후 실패·직접 영향받는 경로만 검사한다. 기대값·대기 시간·측정 방식만 수정했다면 해당 검사만 재실행한다. 성공했고 영향 없는 검사는 반복하지 않는다.
- 공통 코드·의존성·환경 변화나 구체적인 불확실성으로 기존 성공 근거가 무효일 때만 범위를 넓힌다. 이유와 영향 범위를 먼저 기록한다.
- 명령·환경·실제 결과·미확인 사항을 구분한다. 출력은 통과·실패 건수와 실패 상세로 제한하고, 이번 검사와 재사용한 근거를 구분한다. 필요한 사용자 확인은 절차와 확인점을 안내한다.
- 임시 검증 스크립트·결과는 지정 작업트리나 승인된 임시 경로에 두고 검토·보완·자료 반영까지 보존한다. 근거 인계와 재사용이 끝나면 정리하며 버전 관리에는 넣지 않는다.

## 자료와 도구

- 핵심 코드에는 개념·역할·선택 이유를 떠올릴 짧은 한국어 주석만 둔다. 자료 작성 단계에서 [학습자료 기준](docs/learning/GUIDE.md)을 읽고 따른다.
- PDF·브라우저 실행 전 [도구 환경](docs/tool-environment.md)의 필요한 항목을 읽는다. 기존 실행파일·버전을 확인해 재사용하고 불필요한 설치·업그레이드는 하지 않는다. 도구 변경은 이 프로젝트의 도구 문서에 기록한다.
- 프로젝트 의존성은 프로젝트에, 공통 도구는 문서에 지정한 경로에 둔다. 권한을 우회하는 임시 설치는 하지 않는다. 경로·공유 라이브러리 오류는 문서대로 수정하고 실제 권한 거부는 명령·이유를 보고한다. 도구 실행 권한은 작업 범위 확대 승인이 아니다.

## 완료 보고

해당 단계의 결과만 간결하게 보고한다. 후속 보고는 변경된 결과와 기존 근거 경로만 담는다. `done`은 해당 단계 완료이며 전체 작업 완료와 구분한다.

```text
stage: 위임받은 단계
status: done | blocked | needs_decision
files: 변경 파일과 자료 상태(반영·대기·해당 없음)
verification: 이번 검증 명령·환경·통과/실패 건수·실패 상세, 재사용 근거, 재검증 이유
efficiency: none 또는 `근거 → 반영한 기존 규칙 위치`
open_issues: 실패·미확인·필요한 결정, 없으면 none
```

`efficiency`는 이미 생긴 근거(단계별 사용량·재검증 이유·반복 지시·권한 거부·범위 이탈·되돌림)만으로 한 줄로 적는다. 별도 회고 문서·로그·측정 스크립트나 계측용 호출은 만들지 않는다.
