# B1-2 React 서비스 프로젝트 — Codex 지침

이 프로젝트의 작업 방식과 효율 개선은 이 문서에서 관리한다. 별도 전역 지침 저장소에 의존하지 않는다.

새 프로젝트를 시작할 때 [이관 절차](docs/project-handoff.md)에 따라 운영 지침과 필요한 도구 정보를 계승한다. 과제 조건·진행 상태는 새로 작성하고 기준 저장소·커밋을 기록한다. 개선은 현재 프로젝트에서 관리하며 완료된 프로젝트에 일괄 소급하지 않는다.

## 역할과 진행

- 한국어로 소통하고 사용자 지시를 우선한다. Codex는 범위 결정·Claude Code 위임·소스 검토·커밋을, Claude Code는 구현·검증·학습자료 작성을 맡는다. 지침 관리 작업은 Codex가 직접 처리한다.
- [docs/progress.md](docs/progress.md)의 현재 상태와 해당 체크리스트로 한 번에 하나의 학습 개념을 정하고, 그 개념에 필요한 최소 변경만 진행한다. 환경 구성도 여러 개념을 한 작업으로 묶지 않으며, 해당 개념 밖의 도구·설정을 미리 도입하지 않는다. 학습은 사용자가 자율적으로 진행한다. 작업이 끝나면 해당 개념을 학습한 것으로 간주하고, 이해 확인 질문·설명 요청·학습 완료 승인을 거치지 않고 다음 작업으로 진행한다. 질문만으로 다음 기능을 구현하지 않는다.
- 프로젝트 `CLAUDE.md`와 참조 지침은 Claude가 직접 읽는다. Codex는 사용자가 확인·수정을 요청한 경우에만 읽는다. 학습자료 본문·diff는 재검토하지 않고 반영 여부는 완료 보고로 확인한다.
- 사용자는 구현·검증·학습자료 작성에 필요한 소스와 지침을 Claude Code를 통해 Anthropic API에 전송하는 것을 승인한다. 같은 범위는 매번 확인하지 않는다.
- 루트 README는 사용자가 명시적으로 요청할 때만 읽기·검토·수정한다.

## 프로젝트 공통 조건

- React 18 이상으로 SPA 서비스를 만든다. 시각적 완성도보다 컴포넌트·props·state·비동기 데이터 흐름과 **이벤트 → 상태 변경 → 렌더링**의 이해를 우선한다.
- 구현 요구사항 체크리스트와 진행 상태의 원본은 [docs/progress.md](docs/progress.md)다. 기존 [과제 분석](docs/requirement-analysis.md)은 해석 배경이며, 누락·불명확한 요구사항만 [과제 PDF](docs/Requirement_B1-2.pdf)로 확인한다.
- 서비스는 B1-1·B1-2 학습자료를 담는 학습 기록 CRUD로 제한한다. 기술 구성은 React·JavaScript·Vite·npm·React Router·일반 CSS·Supabase·Vercel이며, 결정 근거와 다음 작업 범위는 `docs/progress.md`를 따른다.
- 최소 5개 페이지 라우트와 Not Found, prop을 받는 재사용 컴포넌트 최소 8개, 원격 CRUD, controlled input, 커스텀 훅을 구현한다. 세부 완료 기준은 진행 체크리스트를 따른다.
- `src/pages`·`src/components`·`src/hooks`·`src/lib`·`src/styles`의 역할을 분리하고 필요한 경로부터 구성한다. B1-1의 순수 JavaScript·라이브러리 금지·소스 구조는 계승하지 않는다.
- TypeScript·반응형·인증·전역 상태·메모이제이션은 필수가 아니다. 스타일링 방식은 자유이며 요구사항 밖의 기능으로 범위를 넓히지 않는다.
- 민감 정보는 환경변수로 관리하고 저장소에 포함하지 않는다. 실제 원격 데이터 변경 검증은 테스트 데이터와 정리 범위를 정해 수행한다.
- UI 동작은 브라우저에서 상태·시간에 따른 변화를 검사한다. 표시·숨김은 속성값뿐 아니라 계산된 스타일·박스 크기로 실제 화면 반영도 확인한다. 구현 단계에서는 정지 화면을 캡처하지 않는다. GIF·영상은 필요한 경우만 검토하고 제출 이미지는 새 과제 요구에 맞춰 준비한다.

## 위임과 검토

- 현재 단계·목표·범위·완료 기준·필요한 검증을 전달한다. 공통 제약은 문서 경로로 참조하고 작업별 추가 조건만 적는다.
- 비대화형 Claude 실행에는 필요한 명령과 일치하는 도구 허용 범위를 지정하고 그 범위를 위임문에도 알린다. 파일 읽기는 Read를 사용하고, 허용한 명령의 다른 표기·복합 명령이나 이미 준비된 디렉터리 생성으로 권한 거부를 반복하지 않는다.
- 구현·필요한 검증 → Codex 소스 검토·보완 → 학습자료·진행 상태 마감 순서로 진행한다. 이 내부 검토를 위해 사용자 승인을 따로 요구하지 않는다.
- 후속 보완은 기록한 세션 ID로 재개한다(`--resume <session_id>`). 최근 세션 자동 선택은 피한다. 맥락이 길어지면 요약과 함께 새 세션으로 넘기고, 자료 작성은 기본적으로 새 세션에 맡긴다.
- 자료 세션에는 개념·관련 파일과 함수/구간·검토된 변경 요약·검증 결과와 근거 경로·미확인 사항만 전달한다. 상세 구성과 본문은 Claude에 맡기되, 작은 작업은 짧게 기록하고 기존 개념은 기존 문서 보완 또는 링크로 연결한다.
- 여러 검증은 시나리오별 선택 실행이 가능하게 요청한다. CSS transition처럼 시간에 따라 바뀌는 값은 완료를 기다린 뒤 계산값으로 비교한다. 보완은 실패·직접 영향받는 경로만 검사하고, 기대값 한 건 수정 때문에 전체를 재실행하지 않는다. 전체 재실행은 기존 성공 근거가 무효인 구체적 이유가 있을 때만 한다.
- 변경 파일 목록·소스 diff·검증 근거를 검토한다. 검증 보고는 통과·실패 건수와 실패 상세만 받고 기존 성공 근거를 재사용한다. 코드 수정에 따른 자료 갱신도 위임한다.

## 사용량과 보고

- 가능한 경우 `--output-format json`으로 결과·세션 ID·usage를 수집한다. 원문은 `/tmp` 작업별 경로, 세션 ID·작업 경로·단계별 사용량·재검증 이유·1회성 효율 관찰 요약은 `~/.local/state/codex-claude/`에 보존한다. 공개 저장소·학습자료·`docs/progress.md`에는 넣지 않는다.
- 입력·캐시 생성·캐시 읽기·출력을 구분하고 출력에 포함된 thinking을 중복 합산하지 않는다. 혼합 단계는 통합으로 표시하며 구독 사용률·절감률을 임의 추정하지 않는다. 계측용 모델 호출·측정 스크립트를 추가하지 않는다.
- 핵심 학습 목적 하나가 작업 단위다. 구현·검증·소스 검토·학습자료·진행 상태 갱신을 마친 뒤 커밋 전에 효율 점검을 1회 한다. 중간 보완은 같은 작업으로 본다.
- 점검은 별도 측정 없이 이미 생긴 근거만 본다. 단계별 사용량, 재검증 이유, 반복 지시·권한 거부·범위 이탈·되돌림과 Claude 완료 보고의 `efficiency` 한 줄이 그 근거다.
- 명백하고 재발 가능한 구조적 비효율은 1회에도 바로 고치고, 원인이 불확실하거나 우발적인 것은 같은 원인을 2회 확인한 뒤 고친다. 반영은 이 프로젝트의 AGENTS.md·CLAUDE.md와 관련 지침에 하며, 확인된 비효율을 해결하도록 기존 규칙을 정리하고 중복 규칙이나 별도 회고 문서를 늘리지 않는다.
- 진행 보고는 새로운 사실·결정·남은 불확실성 중심으로 짧게 한다. 최종 보고는 변경·검증·자료 링크·남은 문제·커밋을 담고 이전 보고를 반복하지 않는다. 개념 강의는 질문할 때 제공한다.

## Git과 도구

- 위임 전 `git status --short`를 확인하고 기존 변경을 임의로 커밋·되돌리지 않는다. 변경이 섞이거나 병행 작업이 있으면 전용 worktree를 사용한다. Claude에는 지정 작업트리·범위에서 커밋 없이 결과를 남기도록 한다.
- 검토한 작업 단위만 `유형: 한국어 설명`으로 커밋한다. 유형은 feat/fix/docs/style/refactor/perf/test/build/ci/chore/revert 중 선택한다. worktree 결과는 원래 브랜치에 반영 후 정리하며 푸시는 사용자 요청 시에만 한다.
- 설치 전 `command -v`와 기존 버전·환경을 확인한다. PDF·브라우저 도구는 [도구 환경](docs/tool-environment.md)의 해당 부분만 읽고 재사용한다. 도구 변경 시 이 문서와 필요한 버전 목록도 갱신한다.
- 공통 Python 도구는 `~/.local/share/codex-tools/venv`, 프로젝트 의존성은 프로젝트 환경에 둔다. `/tmp` 설치는 일회성 실험에만 사용하고 공통 환경 쓰기 권한을 우회해 재설치하지 않는다.
